# 开发约束

先阅读 README.md、DESIGN.md 和 docs/08-architecture-decisions.md。当前仓库交付的是设计基线，不能宣称已有可运行的投屏软件或真机测试结果。

修改需关联 FR、ADR 或测试编号；协议变化同步更新 Schema、样例和测试。不得静默扩展到文件传输、APK 安装、录屏、云服务或自动更新。

不导入其他仓库的个人资料、密钥、日志或许可证决定。只操作本项目自有进程、端口和数据；禁止通过结束全局 ADB 或改写用户全局 .android 简化实现。

用户输入仅在当前授权会话中处理。未知送达结果不自动重发，失焦和断线释放输入，日志不记录正文。

应用编译成功、文档校验通过、真机测试通过分别报告。未执行的测试必须写明；禁止伪造性能和兼容性结果。常规提交不得 force push。
