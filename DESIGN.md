# 软件工程设计总览

文档版本：0.1；日期：2026-09-17；阶段：设计基线。

## 1. 已确认的产品方向

采用 scrcpy + ADB；允许用户首次开启 Android 无线调试并配对；手机免安装独立 App；局域网使用；电脑端极简；支持鼠标操作和键盘输入。

本版进一步给出的 Windows 版本范围、默认画质、超时、快捷键、目录、IPC 和测试阈值，是工程设计默认值。真机验证后允许通过架构决策记录调整，不冒充用户已经逐项确认的要求。

## 2. 核心方案

C# / WinUI 3 负责设备页、配对向导、设置和中文文字面板。独立的定制 scrcpy 原生进程负责显示、声音及输入；受控 ADB 实例负责安全无线连接。管理界面不直接解析 scrcpy 私有媒体协议。

第一版保留独立原生投屏窗口，不依赖跨进程 `SetParent` 嵌套视频窗口。需要实际开发的引擎改造包括本地 IPC、结构化状态、焦点释放、显式文字输入、文件拖放禁用和只读模式边界。未经改造的 scrcpy 启动器只能作为技术验证原型。

## 3. 默认值

| 项目 | 设计默认值 |
| --- | --- |
| Windows | Windows 10 22H2 与 Windows 11，x64；以实际测试构建号为准 |
| 手机 | Android 11+，且系统提供并允许无线调试 |
| 会话 | 同时一台；允许保存多个配对记录 |
| 画质 | H.264，长边上限 1920，帧率上限 60，视频码率 8 Mbps |
| 键鼠 | SDK 绝对坐标鼠标 + UHID 键盘 |
| 中文 | 手机输入法直接输入 + 电脑文字面板显式插入 |
| 剪贴板 | 自动同步关闭；跨设备传输必须由用户动作触发 |
| 声音 | 设备输出音频，默认在电脑播放；不采集麦克风 |
| 启动 | 自动发现；不默认自动进入投屏；不开机自启 |
| 重连 | 当前会话异常掉线后，30 秒总预算；不重放输入 |
| Windows 锁定/休眠 | 停止投屏并清理会话；解锁后由用户手动连接 |
| 退出 | 关闭投屏窗口回设备页；退出主程序清理自有进程 |
| 数据 | 本地配置和最小化日志；不采集或上传用户内容 |

## 4. 四个实现边界

**配对成功、ADB 连通、首帧显示分别判定。** 不能将某一步成功直接显示为完整投屏成功。

**协议回执不能证明文字已经进入 App。** 回执丢失时，显示“结果未确认，请检查手机”，禁止自动再插入一次。

**原生窗口与自定义 UI 是两套窗口。** 先保证 DPI、焦点和生命周期，再增加工具栏；不以界面草图替代工程实现。

**只读是本软件的行为限制，不会收窄 ADB 授权。** 删除设备记录也不等于在手机上撤销电脑的调试授权。

## 5. 版本核验结论

2026-09-17 查阅官方发布资料，scrcpy 候选为 v4.1；Android SDK Platform-Tools 候选为 37.0.1。37.0.1 已移除旧 `openscreen` mDNS 后端。候选版本尚未构建和真机验证，不能将候选元数据当作已审核的发布锁文件。参考依据和差异见[参考资料](docs/09-references.md)。

## 6. 交付分界

本次交付的是完整设计文档包及校验辅助脚本。M0 技术验证、Windows 编译、真机联调和安装包发布属于后续实现阶段。每阶段必须提交可核对的证据；文档校验通过不代表投屏功能通过测试。

详细规格从[产品与交互](docs/01-product-and-ux.md)开始阅读。
